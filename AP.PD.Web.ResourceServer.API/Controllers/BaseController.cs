using System.Web.Http;

namespace AP.PD.Web.ResourceServer.API.Controllers
{
    public class BaseController : ApiController
    {
        //protected readonly ResourceRepository ResourceRepository = new ResourceRepository();
    }
}
